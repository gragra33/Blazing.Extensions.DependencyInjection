<UserControl x:Class="WpfExample.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Data Management" 
                       FontSize="20" FontWeight="Bold" 
                       Margin="0,0,0,15"/>
            
            <GroupBox Header="Data Items" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Current items in the data service:"
                               Margin="0,0,0,10"/>

                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding DataItems}"
                             Height="200" 
                             Margin="0,0,0,10"/>

                    <UniformGrid Grid.Row="2" Columns="3" Rows="1">
                        <Button Content="➕ Add Item" 
                                Command="{Binding AddDataItemCommand}"
                                Margin="0,0,5,0"
                                Padding="10,5"/>
                        <Button Content="🔄 Refresh Items" 
                                Command="{Binding LoadDataCommand}"
                                Margin="0,0,5,0"
                                Padding="10,5"/>
                        <Button Content="🗑️ Clear All" 
                                Command="{Binding ClearDataCommand}"
                                Padding="10,5"/>
                    </UniformGrid>
                </Grid>
            </GroupBox>
            
            <Border Background="LightCyan" 
                    Padding="10" 
                    Margin="0,20,0,0"
                    CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="💡 About This View" 
                               FontWeight="Bold" 
                               Margin="0,0,0,5"/>
                    <TextBlock TextWrapping="Wrap">
                        This view demonstrates data binding with an injected DataService. The DataViewModel manages interactions with IDataService, which was automatically resolved by Blazing.Extensions.DependencyInjection.
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
